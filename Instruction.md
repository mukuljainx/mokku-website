Act as an expert frontend developer and UX/UI designer specializing in developer tools. I need you to build a modern, high-converting SaaS landing page for my Chrome extension, "Mokku", which will be hosted at get.mokku.app.

Current Site Context:
The current get.mokku.app site is too basic, largely acting as a GitHub repository mirror, and subpages like the changelog are currently experiencing loading errors. We need to replace this with a dedicated, professional landing page.

Product Overview:
Mokku is an API mocking utility that lives directly inside Chrome DevTools. It helps developers test edge cases by mocking API responses, changing response times, and simulating error states without needing a backend server.

Requirements for the New Website:

1. Tech Stack & Aesthetic:

Use React and Tailwind CSS.

The design must use a premium, developer-focused "dark mode" aesthetic (slate/black backgrounds with vibrant accent colors) similar to Linear or Vercel.

2. Hero Section:

Headline: "Mock API Calls Seamlessly inside Chrome DevTools."

Subheadline: "Stop waiting for the backend. Intercept network requests, write dynamic JavaScript responses, and simulate latency entirely locally."

Primary CTA: "Add to Chrome - Free" (Link to the Chrome Web Store).

Visual: Include a large, realistic mockup of the Chrome DevTools interface with the Mokku tab open.

3. Core Features Section (Grid Layout):
   Please highlight these specific capabilities clearly:

Dynamic Responses: Mention that users can write JavaScript functions in a built-in Monaco Editor to return conditional responses, and that it supports dynamic URL matching using path-to-regexp.

GraphQL Native: Highlight full support for mocking GraphQL queries and mutations.

Header Modification: Explain that users can modify request headers globally or per-URL.

Workspaces: Mention that mocks and headers can be grouped by "Projects" and imported/exported easily.

4. Privacy & Trust Section:

Create a dedicated block emphasizing the "Privacy First" approach. Explicitly state: "Zero telemetry. Mokku does not collect personal information and all mocks are stored securely in your Chrome local store."

Include social proof: Mention the tool is trusted by 15,000+ users and holds a 4.1/5 rating.

5. Footer & Community Links:

Include links to the GitHub repository for bug reports and feature requests.

Include a working link to the Changelog modal/page.

Include a "Support Us" section with links for Buy Me A Coffee and UPI.

Please generate the complete, responsive frontend code for this single-page website.
